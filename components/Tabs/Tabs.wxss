.tabs {
  width: 100%;
  height: 80rpx;
}
.tabs_title {
  display: flex;
  padding: 15rpx 0;
}
.title_item {
  flex: 1;
  text-align: center;
  line-height: 60rpx;
}
.active {
  color:var(--themeColor);
  border-bottom: 6rpx solid var(--themeColor);
}

